.PHONY: test lint clean

test:
	@echo "Configure OCM_TOKEN and target environment before running tests"
	@echo "Usage: make test OCM_TOKEN=<token>"

lint:
	@echo "No linter configured"

clean:
	@echo "Nothing to clean"
